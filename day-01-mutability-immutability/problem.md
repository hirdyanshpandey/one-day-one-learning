# Tricky Interview Problem – Day 01

Predict the output without running the code:

```python
def add_item(item, container=[]):
    container.append(item)
    return container

print(add_item(1))
print(add_item(2))
print(add_item(3))
